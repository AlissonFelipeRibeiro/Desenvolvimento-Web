// src/components/Sobre.js

import React from "react";
import "./Sobre.css"; // Vamos criar o CSS mais tarde

const Sobre = () => {
  return (
    <div className="sobre-container">
      <h2>Sobre o Projeto</h2>
      <p>Este aplicativo foi criado para gerenciar autenticação de usuários e dados usando Firebase.</p>
    </div>
  );
};

export default Sobre;
