// src/components/Home.js

import React from "react";
import "./Home.css"; // Vamos criar o CSS mais tarde

const Home = () => {
  return (
    <div className="home-container">
      <h2>Bem-vindo ao Firebase App!</h2>
      <p className="description">Este é um aplicativo criado para gerenciar autenticação e dados com o Firebase.</p>
    </div>
  );
};

export default Home;
